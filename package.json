{
  "name": "express-k6-profiler",
  "version": "0.1.1",
  "description": "A drop-in dashboard to profile Express bottlenecks, middlewares, and database queries during k6 load testing.",
  "main": "dist/src/main.js",
  "types": "dist/src/main.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/main.d.ts",
      "default": "./dist/src/main.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "cd ui && npm install && npm run build",
    "build": "npm run build:ui && tsc && cp ui/dist/index.html dist/index.html",
    "prepublishOnly": "npm run build",
    "prod": "npm run build && ts-node example/server.ts",
    "dev:mongo": "cd example/ && docker compose up -d && wait-on tcp:3012",
    "dev": "npm run dev:mongo && concurrently -n \"UI ,SVR\" \"npm --prefix ui run dev\" \"cross-env __AS_DEV=true nodemon example/server.ts\""
  },
  "keywords": [
    "express",
    "k6",
    "profiler",
    "performance",
    "monitoring",
    "load-test"
  ],
  "author": "Yonatan",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yonatan-h/express-k6-profiler.git"
  },
  "license": "ISC",
  "type": "commonjs",
  "peerDependencies": {
    "express": "^4.0.0 || ^5.0.0"
  },
  "devDependencies": {
    "@recharts/devtools": "^0.0.11",
    "@types/express": "^5.0.6",
    "@types/node": "^22.19.17",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "express": "^5.2.1",
    "mongoose": "^9.6.1",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "wait-on": "^9.0.5"
  },
  "dependencies": {
    "stacktrace-parser": "^0.1.11"
  }
}
